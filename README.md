# Profiling rails 3.0 and 3.1 startup times

## 3.0

    pushd three-oh-stable && bundle exec ../profile config/environment && popd

## 3.1

    pushd three-one-stable && bundle exec ../profile config/environment && popd